# Test configuration
bot.token=TEST_BOT_TOKEN
bot.username=test_weather_bot

weather.api.key=test_api_key
weather.api.url=http://api.weatherstack.com/current

api.retry.count=1
api.retry.delay.ms=100

cache.ttl.minutes=1
cache.max.size=10
use.cache=true